@model IEnumerable<SmartHup.Models.UserGroup>
@{Dictionary<Tuple<string, string>, bool> menu = ViewBag.menu;
    if (menu == null)
    {menu=new Dictionary<Tuple<string, string>, bool>(); }
 }

@{
    
}


     @if (menu.ContainsKey(Tuple.Create("UserGroups", "Create")))
        {
    <p>
        @Html.ActionLink("Create New", "Create", null, new { @class= "btn btn-success"})
    </p>
    }
    <link href="~/Content/bower_components/datatables.net-bs/css/dataTables.bootstrap.min.css" rel="stylesheet" />
        
    	<div class="box container-fluid">
        <div class="box-header">
            </div>
            <div class="box-body">
                <table  class="table table-bordered table-striped" id="example1" >
    			 <thead>
    			<tr>
            <th>
                @Html.DisplayNameFor(model => model.UserGroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserGroupArName)
            </th>
                    <th>
                        @Html.DisplayNameFor(model => model.GroupType.GroupTypeName)
                    </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
           
            <th>
                @Html.DisplayNameFor(model => model.EntityStatu.EntityStatusName)
            </th>
           
            <th>Actions</th>
        </tr>
    	 <thead>
           <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserGroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserGroupArName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupType.GroupTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
           
            <td>
                @Html.DisplayFor(modelItem => item.EntityStatu.EntityStatusName)
            </td>
          
            <td>
    			@if( menu.ContainsKey(Tuple.Create("UserGroups","Edit")) ) {
    				@Html.ActionLink("Edit", "Edit", new { id=item.systemId },new{ @class = "fa fa-edit" }) 
    			}
    			@if( menu.ContainsKey(Tuple.Create("UserGroups","Details")) ){
    				@Html.ActionLink("Details", "Details", new { id=item.systemId }, new { @class = "fa fa-eye" }) 
    			}
    			@if( menu.ContainsKey(Tuple.Create("UserGroups","Delete")) ){
    				@Html.ActionLink("Delete", "Delete", new { id=item.systemId },new {@class="fa fa-remove"})
    			}
            </td>
        </tr>
    }
    </tbody>
                <tfoot>
    			
      </tfoot>
    </table>
    </div>
    </div>
    <script>
        $(function () {
            $('#example1').DataTable()
           })
    </script>
